# T20 任务报告 - 会话内地图可视化

## 完成内容

- 引入 `maplibre-gl` 5.24.0，并补齐锁文件和本地依赖。
- `show_spatial_map` 已注册到 Agent 公共知识库工具链，并按会话可见知识库校验权限。
- `SpatialMapResult.vue` 支持多图层加载、显隐、顺序、透明度、缩放、平移和自动 bounds。
- 地图结果遵循工具返回的图层顺序、可见性、透明度、颜色和属性字段。
- 点击要素可显示 MapLibre Popup，并同步打开属性抽屉。
- 补齐 Loading、空状态、接口错误、地图错误、超限提示和组件卸载清理。

## 验证

- 空间与迁移目标测试：47 个通过。
- 图谱与工具链目标测试：24 个通过。
- 前端现有 5 个测试脚本：全部通过。
- 前端 ESLint：通过。
- 前端生产构建：通过。
- isolated Web HTTP 200，返回 2026-06-12 新构建 `index-0DLxL5ol.js`。
- 服务端返回的首页资源包含图谱增强检索、知识库迁移、空间数据、MCP 和 Skills。

## 发布状态

本次补齐修复尚未提交、推送或创建新标签。
